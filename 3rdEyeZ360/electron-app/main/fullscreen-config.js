//FullScreen-LockMode
// Common switch for the ActiveExam secured window mode.
// true  = fullscreen/kiosk mode enabled and application switching is blocked.
// false = normal window mode enabled and tabs/applications are switchable for testing.
const ACTIVE_EXAM_FULLSCREEN_ENABLED = false;

module.exports = {
  ACTIVE_EXAM_FULLSCREEN_ENABLED,
};
